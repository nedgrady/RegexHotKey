<?xml version="1.0" encoding="utf-8" ?>
<assemblies>
  <assembly name="Defaults">
    <imports>
      <import>System</import>
      <import>System.IO</import>
      <import>System.Windows.Forms</import>
      <import>System.Runtime.InteropServices</import>
      <import>System.Text.RegularExpressions</import>
      <import>RegexHotKey</import>
    </imports>
    <subscribers>
      <subscription methodName="xkcd">
        <regexHandler>
          <regex>^\\d\\s$</regex>
          <clearTimeMS>10000</clearTimeMS>
        </regexHandler>
        <code>
          //System.Diagnostics.Process.Start("https://xkcd.com/" + param + "/");
        </code>
      </subscription>
      <subscription methodName="Manuscript">
        <regexHandler>
          <regex>^(te)(st)$</regex>
          <clearTimeMS>10000</clearTimeMS>
        </regexHandler>
        <code>
          Console.WriteLine("here");
          Console.WriteLine(#0);
          Console.WriteLine(#1);
          Console.WriteLine(#2);
          //System.Diagnostics.Process.Start("https://xkcd.com/" + param + "/");
        </code>
      </subscription>
    </subscribers>
  </assembly>
</assemblies>
